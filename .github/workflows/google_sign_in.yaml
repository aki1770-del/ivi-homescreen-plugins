name: "google_sign_in"

on:
  pull_request:
    paths:
      # in plugins/[config stuff]
      - "plugins/.clang-*"
      - "plugins/CMakeLists.txt"
      - "plugins/*.cmake"
      - "plugins/*.h"
      - "plugins/*.cpp"
      - "plugins/*.cc"
      - "plugins/*.c"
      - "plugins/*.cc.in"
      # plugin path
      - "plugins/common/**"
      - "plugins/google_sign_in/**"
      # patches
      - "patches/**/*.patch"
      # workflow definition itself
      - ".github/workflows/google_sign_in.yaml"
      - ".github/workflows/_plugin_cpp-ci.yaml"
      - ".github/actions/homescreen-build/**"
  push:
    branches:
      - main

jobs:
  google_sign_in:
    uses: ./.github/workflows/_plugin_cpp-ci.yaml
    with:
      name: "google_sign_in"
      