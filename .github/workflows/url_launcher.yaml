name: "url_launcher"

on:
  pull_request:
    paths:
      # in plugins/[config stuff]
      - "plugins/.clang-*"
      - "plugins/CMakeLists.txt"
      - "plugins/*.cmake"
      - "plugins/*.h"
      - "plugins/*.cpp"
      - "plugins/*.cc"
      - "plugins/*.c"
      - "plugins/*.cc.in"
      # plugin path
      - "plugins/common/**"
      - "plugins/url_launcher/**"
      # patches
      - "patches/**/*.patch"
      # workflow definition itself
      - ".github/workflows/url_launcher.yaml"
      - ".github/workflows/_plugin_cpp-ci.yaml"
      - ".github/actions/homescreen-build/**"
  push:
    branches:
      - main

jobs:
  url_launcher:
    uses: ./.github/workflows/_plugin_cpp-ci.yaml
    with:
      name: "url_launcher"
      